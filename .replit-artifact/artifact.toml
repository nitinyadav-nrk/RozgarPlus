kind = "web"
previewPath = "/"
title = "JobNest"
version = "1.0.0"
id = "artifacts/jobnest"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24884

[services.development]
run = "pnpm --filter @workspace/jobnest run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/jobnest", "run", "build" ]
publicDir = "artifacts/jobnest/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24884"
BASE_PATH = "/"
